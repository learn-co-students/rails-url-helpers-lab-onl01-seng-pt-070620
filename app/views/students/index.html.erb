<%@students.each do |student|%>
<div><a href='<%="/students/#{student.id}"%>'> <%=student.to_s%></a></div>
<%end%>

<%@students.each do |student|%>
<div><%=link_to student.to_s, "/students/#{student.id}"%></div>
<%end%>
<!--
<%@students.each do |student|%>
<div><%=link_to student.to_s, students_path(student.id)%></div>
<%end%>

<%@students.each do |student|%>
<div><%=link_to student.to_s, students_path(student)%></div>
<%end%> -->